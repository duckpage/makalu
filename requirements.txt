Django
Psycopg2-binary
Pillow
WeasyPrint
#signxml


# Ubuntu/Debian 
# sudo apt install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info
# sudo apt install python3-dev python3-cffi libxml2-dev libxslt1-dev libssl-dev python3-lxml python3-cryptography python3-openssl python3-certifi python3-defusedxml

# macOs
# brew install python3 cairo pango gdk-pixbuf libffi
# xcode-select --install; brew install openssl; export LDFLAGS="-L$(brew --prefix openssl)/lib" CFLAGS="-I$(brew --prefix openssl)/include"